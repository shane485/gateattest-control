# gateattest-control

Public control fixture for **Gate Attestation**. The CI behaviours here are
planted ON PURPOSE (a silently-skipped security matrix under a green required
aggregator, a neutral check, an intermittent absence via paths-ignore, a
renamed required check, and a workflow disabled mid-history) to validate the
scanner against a known answer key. Nothing here is a real defect.
